# dependencies
node_modules/

# this package uses pnpm; ignore stray npm/yarn lockfiles
package-lock.json
yarn.lock

# build output
dist/
*.tsbuildinfo

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# env
.env
.env.*
!.env.example

# test coverage
coverage/

# debug logs
*.log
npm-debug.log*
pnpm-debug.log*

# superpowers (auto-installed brainstorm artifacts; do not track)
.superpowers/
docs/superpowers/

# agent tooling — session worktrees live under .claude/worktrees/
.claude/

# pnpm writes this on install just to record build-script approvals
pnpm-workspace.yaml

# claude code (지침 문서 · 로컬 설정 · 세션 워크트리 · 자동 설치 스킬 · MCP 설정)
CLAUDE.md
.claude.json
.claude.json.*
.claude-*.json

# axt 로컬 프로파일 (백업본 포함)
.axt/
.axt*

# ast (agent session tracker) 로컬 산출물
.ast/
.ast*
